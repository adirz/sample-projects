/**
 * The Main class initializes a new XO game and starts it.
 */
class Main {

    /** Initializes a new game and starts it. */    
    function void main() {
        var XO game;

        let game = XO.new();
        do game.run();
        do game.dispose();

        return;
    }
}
